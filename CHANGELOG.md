# Changelog

本项目的版本变更记录。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.0] - 2026-09-13

一次大规模功能更新：从"音乐播放器专用"升级为「**音乐 + 任意程序**」的 Steam 状态同步。

### 新增

- **程序同步（全新）**：把任意程序/游戏同步到 Steam 状态
  - 前台程序检测（微秒级开销，只读进程名/窗口标题，不读内存，反作弊环境安全）
  - 程序分类（忽略 / 游戏 / 工作 / 媒体 / 社交 / 其他）+ 单程序策略覆盖
  - 自动发现新程序：内置约 50 个常见程序字典 + 全屏检测智能建议分类
  - "运行即显示"模式（挂机程序持续显示）
  - 程序同步设置窗口：总开关、规则列表（显示名/分类/模式）、"添加当前前台程序"
- **积木编辑器**：模板可视化编辑——无限添加积木块（歌名/歌手/进度条/程序名/分隔符/自定义文字），
  ↑↓ 排序、实时预览，告别手写变量
- **模板预设**：简洁 / 带前缀（正在玩 · 正在听）/ 只要名字，一键切换
- **进度条样式自定义**：预设样式 + 任意字符（含 emoji）
- **显示模板引擎**：程序与音乐组合显示，分隔符可选可自定义
- **音乐 / 程序独立开关**：分别控制两类状态的同步；"暂停时自动隐藏音乐"
- **音乐播放器优先级可配置**：设置内调整顺序，首页与同步即时生效
- **首页重构**：3 个固定面板 → 动态「音乐」（跟随当前活跃源）+「程序」双面板，
  新增 Steam 连接状态标签、程序图标显示
- 双击歌曲名打开歌曲页面；托盘实时显示播放状态 + 「暂停同步」一键开关
- 登录窗内置手机验证步骤引导（Steam 客户端 / 实际所在地）

### 改进

- 进度刷新 1 秒 → **0.5 秒**，Steam 端滚动更平滑
- 状态文本上限 63 → **128 字节**，智能降级截断（优先保程序名）
- 登录错误提示全中文化（密码错误 / 令牌问题等明确区分）
- **连接健壮性大幅增强**：
  - TCP 连接失败自动回退 **WebSocket (443 端口)**
  - 服务器列表本地持久化缓存（官方接口不可达时仍可连接）
  - 服务器要求换节点（TryAnotherCM）自动重试，最多 4 轮
  - 令牌不再因超时/网络波动被误清
- **发布方式**：提供免安装单文件（内置 .NET 运行时，双击即用）+ 轻量版双产物
- 应用图标替换为小王子插画（圆形多尺寸 ICO）

### 修复

- 前台检测 P/Invoke 缺失 Unicode 后缀导致主轮询循环异常（界面"识别-清空"闪烁）
- 登录窗句柄竞态（旧窗口销毁后收到事件回调导致"在创建窗口句柄之前"异常）
- **配置被重置问题**：读取兼容性问题 + 写入中断风险 → 改为原子写盘 +
  宽容解析（任意格式枚举值均可读取）+ 坏文件自动归档
- 积木编辑器在高 DPI 下按钮被窗口裁剪

### 工程

- 新增 [SECURITY.md](SECURITY.md)：登录流程 / 数据流向 / 风险与撤销方法完整交代
- 单元测试 17 → 35 个（配置兼容性、宽容读取、分类器、保存链路等）
- CI 双产物构建 + Release notes 自动从 CHANGELOG 提取
- .gitignore 加固（本地配置 / 日志 / 缓存永不入库）

## [0.1.0] - 2026-09-07

首个公开发布版本。MuSync 是 [wuyan1337/yySync](https://github.com/wuyan1337/yySync) 的
「半新写」重构版：播放器内存逆向实现沿用原项目（MIT，见 [THIRD-PARTY-NOTICES](THIRD-PARTY-NOTICES.md)），
以下为相对 yySync 的全部改进。

### 修复

- **断线重连后不再自动重新登录的问题**：断开后仅重连不重登，同步静默失效直至重启。
  现在指数退避重连（5s → 60s），连接恢复后自动用保存的令牌重新登录。
- **启动时连接失败永不重试**：新增 20 秒 watchdog，首次连接不成功自动转入退避重连。
- **令牌重登途中断线会误删有效令牌**：现在断线保留令牌，重连成功后自动重试。
- **错误状态映射混乱**：
  - `DllNotFound`（播放器启动竞态）误报「需要管理员运行」→ 5 秒宽限期后提示「播放器组件未加载」；
  - 特征码失效（`EntryPointNotFound`）被吞成无错误、界面提示永远不会出现 → 正确提示「版本不支持/特征码失效」；
  - `OpenProcess` 拒绝访问无法识别 → 明确提示「需要管理员运行」。
- **多播放器状态冲突**：关闭任一播放器会无条件清空 Steam 状态；暂停中的播放器会霸占进度刷新。
  改为活跃源仲裁：正在播放的优先，关闭自动切换，不再误清状态。
- **封面图生命周期隐患**：切歌瞬间旧封面可能被缓存淘汰后仍被界面引用。
- **配置读取失败会直接覆盖丢失**：损坏时先备份 `config.json.bak`。
- **登录窗口「记住我」复选框无实际作用**：现在不勾选则不落盘令牌与 Guard 数据。

### 新增

- **真实游戏自动暂停**：检测到 Steam 客户端正在玩真实游戏时自动清除并暂停音乐状态，
  退出游戏自动恢复（设置中可开关，默认开启）。
- **托盘状态与手动控制**：悬停提示与右键菜单实时显示播放/暂停状态（约 5 秒刷新）；
  新增「暂停同步」手动开关。
- **主界面 Steam 会话状态标签**：未启用 / 连接中 / 未登录 / 游戏中暂停 / 同步中。
- **双击歌曲标题打开歌曲页**（浏览器）。
- **设置窗口「重新登录」入口**：令牌失效后无需重启程序。
- **Release 文件日志**：关键事件（连接/登录/重连/错误）写入
  `%LocalAppData%\MuSync\logs\`，按天滚动；稳定错误仅在首次发生时记录避免刷屏。
- **token 使用 Windows DPAPI 加密存储**（带前缀，兼容旧明文配置自动迁移）。
- **xUnit 单元测试**（14 个）：UTF-8 截断边界、状态格式化、优先级策略、DPAPI 往返等。
- **GitHub Actions CI**：构建 + 测试 + 单文件 Release 产物（`MuSync-win-x64`）。

### 优化

- LX Music HTTP 轮询从每秒约 4 次降为 1 次，请求失败沿用缓存避免误清状态。
- 无播放器运行时轮询从 233ms 退避至 1.2s，托盘驻留时停止主窗口 UI 刷新。
- 启动不再阻塞：先显示界面，后台自动登录，失败才弹登录窗口。
- 删除生产环境 `GC.Collect` 内存卫士全家桶（强制 GC、内存快照、反射计数）。
- 登录窗口中文化。
- 性能监控面板每 2 秒自动刷新，信息扩充（虚拟内存 / GC / 分类缓存计数）。

### 工程

- 项目更名 MuSync，清理 csproj 上游残留元数据与无用依赖（Newtonsoft.Json 等）。
- 解决方案拆分（主程序 + `tests/MuSync.Tests`），新增 `InternalsVisibleTo`。
- 完整 README、LICENSE（MIT）、THIRD-PARTY-NOTICES、Changelog。
